"use strict";
var MVPresenter = MVPresenter || {
	/** Sub Namespaces **/
	//Even though we're doing 'Presenter' pattern here, we'll keep the namespace "Controller"
	//It'll make it easier to compare the projects if we keep the directory structure
	//the same
	Application:{},
	Controller:{},
	Model:{},
	View:{
		Component:{}
	}
}
